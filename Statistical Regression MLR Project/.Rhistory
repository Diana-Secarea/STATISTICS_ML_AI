library(readxl)
FINAL_PROJECT_CLEARED <- read_excel("FINAL PROJECT CLEARED.xlsx",
sheet = "Sheet1")
View(FINAL_PROJECT_CLEARED)
colnames(df)[2:7] <- c("Unemployment",
"PopDensity",
"AgriShare",
"GDP",
"Education",
"CEE_Dummy");
head(df)
df <- FINAL_PROJECT_CLEARED
colnames(df)[2:7] <- c("Unemployment",
"PopDensity",
"AgriShare",
"GDP",
"Education",
"CEE_Dummy")
head(df)
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
head(model_a)
summary(model_a)
#Restricted model, does not include CEE Dummy
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
summary(model_b)
#Deletion of the stored data
rm(df)
rm(FINAL_PROJECT_CLEARED)
rm(model_a)
rm(model_b)
#Start from scratch using a specialised library to load the excel file
library(readxl)
#Import the data with the correct excel read approach
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Sheet1", col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
#verify the data
head(df)
head(df$PopDensity)
head(df$1)
head(df$`Population Density`)
#Renaming the columns
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
#Full model, includes CEE Dummy
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
summary(model_a)
#Restricted model, does not include CEE Dummy
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
summary(model_b)
#Check for the car library
if(!require(car)) install.packages("car")
#Check for the car library
if(!require(car)) install.packages("car")
#Check for the car library
if(!require(car)) install.packages("car")
#Checking Multicolliniarity
vif(model_a)
#dividing the window into 4 so we can see all 4 graphs at the same time
par(mfrow=c(2,2))
#generating the diagnostic charts
plot(model_a)
#generating the diagnostic charts
plot(model_a)
#Descriptive statistics
summary(df[2:6])
sapply(df[2:6], sd)
#Saphiro Wilk Test
shapiro.test(residuals(model_a))
#Homoscedasticity
bptest(model_a)
#Load library for Breuch-Pagan test
if(!require(lmtest)) install.packages("lmtest")
#Load library for Breuch-Pagan test
if(!require(lmtest)) install.packages("lmtest")
library(lmtest)
#Homoscedasticity
bptest(model_a)
#Data cleaning check
sum(is.na(df))
if(!require(readxl)) install.packages("readxl")
if(!require(car)) install.packages("car")      # For VIF
if(!require(lmtest)) install.packages("lmtest") # For BP and DW tests
library(readxl)
library(car)
library(lmtest)
#Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
if(sum(is.na(df)) > 0) {
print("Warning: Missing values were removed from the dataset.")
}
print("DESCRIPTIVE STATISTICS: SUMMARY")
summary(df[2:6])
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
print("--- MODEL A (WITH DUMMY) RESULTS ---")
summary(model_a)
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
print("--- MODEL B (WITHOUT DUMMY) RESULTS ---")
summary(model_b)
print("--- TEST 1: COLLINEARITY (VIF) ---")
vif(model_a)
# B. Normality of Errors (Shapiro-Wilk Test)
print("--- TEST 2: NORMALITY (Shapiro-Wilk) ---")
# H0: Errors are normally distributed. (We want p-value > 0.05)
shapiro.test(residuals(model_a))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_a)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_a)
# Visual confirmation of the tests above
par(mfrow=c(2,2)) # Split view into 4 quadrants
plot(model_a)
# Cleaning the missing values
if(sum(is.na(df)) > 0) {
df <- na.omit(df)
print("Warning: Missing values were removed from the dataset.")
}
summary(df[2:6])
plot(model_a)
clear
cls
sapply(df[2:6], getmode)
uniqv[which.max(tabulate(match(v, uniqv)))]
#Libraries
if(!require(readxl)) install.packages("readxl")
if(!require(car)) install.packages("car")      # For VIF
if(!require(lmtest)) install.packages("lmtest") # For BP and DW tests
library(readxl)
library(car)
library(lmtest)
getmode <- function(v) {
uniqv <- unique(v)
uniqv[which.max(tabulate(match(v, uniqv)))]
}
#------------------------------------------------------------------------------------------------------------------------------
# Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
# Cleaning the missing values
if(sum(is.na(df)) > 0) {
df <- na.omit(df)
print("Warning: Missing values were removed from the dataset.")
}
#------------------------------------------------------------------------------------------------------------------------------
# Descriptive
print("DESCRIPTIVE STATISTICS: SUMMARY")
summary(df[2:6])
print("DESCRIPTIVE STATISTICS: MODE")
sapply(df[2:6], getmode)
# Model A: Full model (Includes CEE_Dummy)
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
print("---FULL MODEL (WITH DUMMY) RESULTS---")
summary(model_a)
# Model B: Restricted model (No Dummy)
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
print("---RESTRICTED MODEL (WITHOUT DUMMY) RESULTS---")
summary(model_b)
#------------------------------------------------------------------------------------------------------------------------------
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_a)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_a))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_a)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_a)
#------------------------------------------------------------------------------------------------------------------------------
# Visual confirmation of the tests above
par(mfrow=c(2,2)) # Split view into 4 quadrants
plot(model_a)
par(mfrow=c(1,1)) # Reset view
#Libraries
if(!require(readxl)) install.packages("readxl")
if(!require(car)) install.packages("car")      # For VIF
if(!require(lmtest)) install.packages("lmtest") # For BP and DW tests
library(readxl)
library(car)
library(lmtest)
getmode <- function(v) {
uniqv <- unique(v)
uniqv[which.max(tabulate(match(v, uniqv)))]
}
#------------------------------------------------------------------------------------------------------------------------------
# Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
#------------------------------------------------------------------------------------------------------------------------------
# Descriptive
print("DESCRIPTIVE STATISTICS: SUMMARY")
summary(df[2:6])
print("DESCRIPTIVE STATISTICS: MODE")
sapply(df[2:6], getmode)
# Model A: Full model (Includes CEE_Dummy)
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
print("---FULL MODEL (WITH DUMMY) RESULTS---")
summary(model_a)
# Model B: Restricted model (No Dummy)
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
print("---RESTRICTED MODEL (WITHOUT DUMMY) RESULTS---")
summary(model_b)
#------------------------------------------------------------------------------------------------------------------------------
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_a)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_a))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_a)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_a)
#------------------------------------------------------------------------------------------------------------------------------
# Visual confirmation of the tests above
par(mfrow=c(2,2)) # Split view into 4 quadrants
plot(model_a)
par(mfrow=c(1,1)) # Reset view
#Libraries
if(!require(readxl)) install.packages("readxl")
if(!require(car)) install.packages("car")      # For VIF
if(!require(lmtest)) install.packages("lmtest") # For BP and DW tests
library(readxl)
library(car)
library(lmtest)
getmode <- function(v) {
uniqv <- unique(v)
uniqv[which.max(tabulate(match(v, uniqv)))]
}
# Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
# Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
print("DESCRIPTIVE STATISTICS: SUMMARY")
summary(df[2:6])
Model A: Full model (Includes CEE_Dummy)
# Model A: Full model (Includes CEE_Dummy)
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
print("---FULL MODEL (WITH DUMMY) RESULTS---")
summary(model_a)
# Model B: Restricted model (No Dummy)
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
print("---RESTRICTED MODEL (WITHOUT DUMMY) RESULTS---")
summary(model_b)
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_a)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_a))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_a)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_a)
# Visual confirmation of the tests above
par(mfrow=c(2,2)) # Split view into 4 quadrants
plot(model_a)
par(mfrow=c(1,1)) # Reset view
#Libraries
if(!require(readxl)) install.packages("readxl")
if(!require(car)) install.packages("car")      # For VIF
if(!require(lmtest)) install.packages("lmtest") # For BP and DW tests
library(readxl)
library(car)
library(lmtest)
getmode <- function(v) {
uniqv <- unique(v)
uniqv[which.max(tabulate(match(v, uniqv)))]
}
#------------------------------------------------------------------------------------------------------------------------------
# Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
#------------------------------------------------------------------------------------------------------------------------------
# Descriptive
print("DESCRIPTIVE STATISTICS: SUMMARY")
summary(df[2:6])
print("DESCRIPTIVE STATISTICS: MODE")
sapply(df[2:6], getmode)
# Model A: Full model (Includes CEE_Dummy)
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
print("---FULL MODEL (WITH DUMMY) RESULTS---")
summary(model_a)
# Model B: Restricted model (No Dummy)
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
print("---RESTRICTED MODEL (WITHOUT DUMMY) RESULTS---")
summary(model_b)
#------------------------------------------------------------------------------------------------------------------------------
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_a)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_a))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_a)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_a)
#------------------------------------------------------------------------------------------------------------------------------
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_b)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_b))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_b)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_b)
#------------------------------------------------------------------------------------------------------------------------------
# Visual confirmation of the tests above
par(mfrow=c(2,2)) # Split view into 4 quadrants
plot(model_a)
par(mfrow=c(1,1)) # Reset view
#Libraries
if(!require(readxl)) install.packages("readxl")
if(!require(car)) install.packages("car")      # For VIF
if(!require(lmtest)) install.packages("lmtest") # For BP and DW tests
library(readxl)
library(car)
library(lmtest)
getmode <- function(v) {
uniqv <- unique(v)
uniqv[which.max(tabulate(match(v, uniqv)))]
}
#------------------------------------------------------------------------------------------------------------------------------
# Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
#------------------------------------------------------------------------------------------------------------------------------
# Descriptive
print("DESCRIPTIVE STATISTICS: SUMMARY")
summary(df[2:6])
print("DESCRIPTIVE STATISTICS: MODE")
sapply(df[2:6], getmode)
# Model A: Full model (Includes CEE_Dummy)
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
print("---FULL MODEL (WITH DUMMY) RESULTS---")
summary(model_a)
# Model B: Restricted model (No Dummy)
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
print("---RESTRICTED MODEL (WITHOUT DUMMY) RESULTS---")
summary(model_b)
#------------------------------------------------------------------------------------------------------------------------------
print("---MODEL A---")
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_a)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_a))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_a)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_a)
#------------------------------------------------------------------------------------------------------------------------------
print("---MODEL A---")
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_b)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_b))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_b)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_b)
#------------------------------------------------------------------------------------------------------------------------------
# Visual confirmation of the tests above
par(mfrow=c(2,2)) # Split view into 4 quadrants
plot(model_a)
par(mfrow=c(1,1)) # Reset view
#Libraries
if(!require(readxl)) install.packages("readxl")
if(!require(car)) install.packages("car")      # For VIF
if(!require(lmtest)) install.packages("lmtest") # For BP and DW tests
library(readxl)
library(car)
library(lmtest)
getmode <- function(v) {
uniqv <- unique(v)
uniqv[which.max(tabulate(match(v, uniqv)))]
}
#------------------------------------------------------------------------------------------------------------------------------
# Data import
df <- read_excel("FINAL PROJECT CLEARED.xlsx", sheet = "Data",
col_types = c("text", "numeric", "numeric", "numeric", "numeric", "numeric", "numeric", "text"))
colnames(df)[2:7] <- c("Unemployment", "PopDensity", "AgriShare", "GDP", "Education", "CEE_Dummy")
#------------------------------------------------------------------------------------------------------------------------------
# Descriptive
print("DESCRIPTIVE STATISTICS: SUMMARY")
summary(df[2:6])
print("DESCRIPTIVE STATISTICS: MODE")
sapply(df[2:6], getmode)
# Model A: Full model (Includes CEE_Dummy)
model_a <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education + CEE_Dummy, data = df)
print("---FULL MODEL (WITH DUMMY) RESULTS---")
summary(model_a)
# Model B: Restricted model (No Dummy)
model_b <- lm(Unemployment ~ PopDensity + AgriShare + GDP + Education, data = df)
print("---RESTRICTED MODEL (WITHOUT DUMMY) RESULTS---")
summary(model_b)
#------------------------------------------------------------------------------------------------------------------------------
print("---MODEL A---")
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_a)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_a))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_a)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_a)
#------------------------------------------------------------------------------------------------------------------------------
print("---MODEL B---")
# A. Collinearity (Variance Inflation Factor)
print("---COLLINEARITY---")
vif(model_b)
# B. Normality of Errors (Shapiro-Wilk Test)
print("---NORMALITY ---")
shapiro.test(residuals(model_b))
# C. Homoscedasticity (Breusch-Pagan Test)
print("---HOMOSCEDASTICITY---")
bptest(model_b)
# D. Autocorrelation (Durbin-Watson Test)
print("---AUTOCORRELATION---")
dwtest(model_b)
#------------------------------------------------------------------------------------------------------------------------------
# Visual confirmation of the tests above
par(mfrow=c(2,2)) # Split view into 4 quadrants
plot(model_a)
par(mfrow=c(1,1)) # Reset view
